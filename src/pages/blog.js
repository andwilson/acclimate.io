import React from "react";
import Helmet from "react-helmet";

export default () => (
  <div>
    <Helmet>
      <title>Acclimate | Blog</title>
    </Helmet>
    <div>
        <h1>Blog page</h1>
    </div>
  </div>
);
