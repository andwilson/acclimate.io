import React from "react";
import Helmet from "react-helmet";

export default () => (
  <div>
    <Helmet>
      <title>Acclimate | Insights</title>
    </Helmet>
    <div>
        <h1>Insights page</h1>
    </div>
  </div>
);
